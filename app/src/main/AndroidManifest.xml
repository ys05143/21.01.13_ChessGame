<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chessgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/chess_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/chess_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChessGame">
        <activity android:name=".Ai_b_Activity2"></activity>
        <activity android:name=".Ai_w_Activity" />
        <activity android:name=".Ai_b_Activity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>